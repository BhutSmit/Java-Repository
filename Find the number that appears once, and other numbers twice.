class AppearOne{

  public static void main(String [] args){

        //size of the array:
        int n = arr.length;

        // XOR all the elements:
        int xorr = 0;
        for (int i = 0; i < n; i++) {
            xorr = xorr ^ arr[i];
        }
        return xorr;
    
  }
}

=======================================
Example:

arr = [2, 2, 1]

ans 1
